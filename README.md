# ERT-Whitepaper (LaTeX)
- Kompilieren: `make` (benötigt latexmk + biber)
- Inhalte in `content/`, Abbildungen in `figs/`, Bib in `bib/`.
- Zentrale Makros: `\alphastar` etc. in `preamble/macros.tex`.
